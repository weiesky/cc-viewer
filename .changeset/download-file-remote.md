---
"cc-viewer": patch
---

feat(files): remote (non-127.0.0.1) sessions get a "Download to local" item in the file-explorer right-click menu, streamed through the new `GET /api/download-file` attachment endpoint.
