# M1.4: 细节修复与上下文结构化 — 路线图

**版本**: v1.4
**状态**: 进行中
**Phases**: 2 (Phase 18-19)

---

## Phase 18: iframe URL 状态持久化

**目标**: 修复可视化编辑器模式切换时 iframe URL 丢失的 bug

**任务**:
- [ ] 定位 `previewUrl` 状态管理位置（`App.jsx` / `PagePreview.jsx`）
- [ ] 在切换 `viewMode`（visual ↔ 其他）时保留 `previewUrl` 而非重置
- [ ] 确保刷新/重开 visual 模式后 URL 恢复到上次输入值

**关键文件**:
- `src/App.jsx`
- `src/components/VisualEditor/PagePreview.jsx`

**预计改动量**: 小

---

## Phase 19: XML 结构化元素上下文替代硬编码 prompt

**目标**: 将 `buildElementContext()` 输出从自然语言 prompt 改为 XML 结构化格式

**任务**:
- [ ] 重构 `src/utils/elementContext.js`，使用 `<selected-element>` XML 标签封装数据
- [ ] 字段包含：截图路径、组件名、tag、className、id、文件路径、选择器、文本内容、计算样式
- [ ] 更新 `TerminalPanel.jsx` 中的组合逻辑：用户 prompt 与元素上下文分离
- [ ] 确认 AI 收到的最终输入格式清晰、可扩展

**关键文件**:
- `src/utils/elementContext.js`
- `src/components/TerminalPanel.jsx`

**预计改动量**: 中

**依赖**: Phase 18（完成后再做）

---

## 里程碑成功标准

1. 切换视图模式后再回到 visual，iframe URL 保留上次输入值
2. `buildElementContext()` 输出 XML 格式，不包含硬编码中文指令
3. 终端发送内容：`<selected-element>...</selected-element>\n{用户输入}`
4. 全部测试通过
