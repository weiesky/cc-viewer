node_modules
dist
electron-dist
coverage
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/worktrees/
.claude/skills/
.claude/skills-skip/
.idea/
.DS_Store
.omc/
.omo/
tmp/

# Sensitive signing-related files
*.p12
*.cer
*.certSigningRequest
*.pem
*.key
*.pfx
.env
.env.*
!.env.example

# Local agent instruction mirror (copy of CLAUDE.md, kept out of repo)
AGENTS.md

# Workspace system prompt sentinels (written at runtime by expert settings / CCV, kept out of repo)
CC_SYSTEM.md
CC_APPEND_SYSTEM.md

# Claude Code decompiled artifacts (main JS bundle extracted from official bun --compile binary, reference only, kept out of repo)
claude-code-*.cli.js
system_prompt/
# Working analysis documents (session artifacts, not deliverables)
code-rot-analysis.md
